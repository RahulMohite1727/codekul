export interface UserData {
    id: string;
    first_name: string;
    last_name: string;
    avatar: string;
}

export interface UserDataService{
    id: string 
    first_name: string,
     last_name: string,
     avatar: string
}